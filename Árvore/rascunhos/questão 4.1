#include <stdio.h>
#include <stdlib.h>

struct nodo
{
  int valor;
  struct nodo *esq;
  struct nodo *dir;
} typedef Nodo;

Nodo *raiz = NULL; 

void separador(){
  printf("\n------------------------\n");
}

Nodo *criar(int valor){
  Nodo *n = malloc(sizeof(Nodo));
  n->valor = valor;
  n->esq = NULL;
  n->dir = NULL;
  return n;
}

void add(Nodo *n, int valor){
  if (valor < n->valor){
    if(n->esq == NULL){
      n->esq = criar(valor);
    } else {
      add(n->esq, valor);
    }
  } else {
    if (n->dir == NULL){
      n->dir = criar(valor);
    } else {
      add(n->dir, valor);
    }
  }
}

void imprimir(Nodo *n){
  if(n==NULL)
		return;
	imprimir(n->esq);
	printf("\n%d",n->valor);
	imprimir(n->dir);
}

int main() {
  int fechar=0, op, valor;
  char aux_case1= 's';

  do {
    separador();
    printf("1_Adicionar valor\n2_remover valor\n3_listar valores\n4_calcular nós da arvora\n5_calcular autura\n6_achar maior numero");
    printf("\n7_fechar");
    separador();
    printf("opção:\t");
    scanf("%d", &op);
    switch (op){
      case 1:
        printf("\nInforme o valor\t");
        scanf("%d", &valor);

        if (raiz == NULL){
          raiz = criar(valor);
        } else {
          add(raiz, valor);
        }
      break;
      case 2:
      break;
      case 3:
      imprimir(raiz);
      break;
      case 4:
      break;
      case 5:
      break;
      case 6:
      break;
      case 7:
      fechar = 1;
      printf("fechando programa.");
      break;
    }
  }while (fechar !=1);
  return 0;
}
