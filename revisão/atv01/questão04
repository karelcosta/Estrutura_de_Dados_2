#include <stdio.h>

int main(void) {
  float preco;
  printf("infome o preço:\t");
  scanf("%f", &preco);

  if (preco < 100){
    preco = preco * 1.1;
  } else if (preco >= 100){
    preco = preco * 1.2;
  }
   printf("preço inflacionado = %.2f", preco);
  
  return 0;
}
