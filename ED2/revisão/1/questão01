#include <stdio.h>

int main(void) {
  float valor;
  
  printf("informe o valor em metros:");
  scanf("%f", &valor);

  printf("decimetros = %.3f \ncentimetros = %.3f \nmilimetros = %.3f", valor/10, valor/100, valor/1000);

  return 0;
}
